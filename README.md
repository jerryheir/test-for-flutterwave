# test-for-flutterwave

### STEP 1
```yarn install``` OR ```npm install```

### STEP 2
```yarn dev``` OR ```npm run dev```

### STEP 3
Open Postman => localhost:5000/

`GET` and `POST` (/validate-rule) requests
